import "std";

class Handler {
    func index(id) {
        std.io.print(id);
        return "Hello from Aura Standard Library!";
    }
}

func main() {
    var h = new Handler();
    std.io.print("Starting namespaced server on 8082...");
    std.net.api_listen(8082, h);
}

main();
